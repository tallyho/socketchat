{
  "name": "socket-chat-server",
  "version": "1.0.0",
  "description": "The back-end server that stores users and chat messages. Connects clients to each other and the database.",
  "main": "index.js",
  "scripts": {
    "start": "babel-node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jesse Tannahill",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^2.1.18",
    "mongoose": "^4.4.14",
    "socket.io": "^1.4.5"
  },
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-es2015": "^6.6.0"
  },
  "babel": {
    "presets": [ "es2015" ]
  }
}
